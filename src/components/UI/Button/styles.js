import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  button: {
    width: 200,
    borderRadius: 5,
    borderColor: 'white',
    borderWidth: 2,
    padding: 10,
    alignItems: 'center',
  },
  text: {
    color: 'white',
    fontSize: 16,
  },
});

export default styles;
